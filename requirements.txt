anthropic>=0.40.0
python-docx>=1.1.0
python-dotenv>=1.0.0
requests>=2.32.0
beautifulsoup4>=4.12.0
playwright>=1.40.0

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
itsdangerous>=2.2.0
sqlalchemy>=2.0.0
pydantic>=2.8.0
email-validator>=2.2.0
bcrypt>=4.0.0
tzdata>=2024.1  # Windows needs this for ZoneInfo; no-op on macOS/Linux
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0

# --- Job discovery + scheduling ---
apscheduler>=3.10           # daily discovery scheduler (in-process)
# numpy/pandas: modern versions ship Python 3.14 wheels; python-jobspy over-pins
# numpy==1.26.3 / pandas<3 which have NO 3.14 wheels, so install jobspy with
# --no-deps and supply compatible versions here. See scripts/setup_env.ps1.
numpy>=2.1
pandas>=2.2
markdownify>=0.13.1         # jobspy runtime dep
regex>=2024.4.28            # jobspy runtime dep
tls-client>=1.0.1           # jobspy runtime dep
# python-jobspy installed separately with --no-deps (see scripts/setup_env.ps1):
#   pip install --no-deps python-jobspy
